아래 git 명령어를 순서대로 실행해줘:

1. `git add .`
2. `git status` 로 변경 파일 목록 확인
3. `git diff --cached` 로 변경 내용을 파악한 뒤, 내용에 맞는 적절한 한국어 커밋 메시지를 자동으로 작성해서 `git commit` 실행
4. `git push`

완료되면 어떤 파일이 푸시됐는지 간략하게 알려줘.
